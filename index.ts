// Program to find the the sum of two by function and call it 3 times. But the result must be different.

function numSum(num1: number, num2: number) {
  // sum

  var result: number = num1 + num2;

  // show
  console.log("result is", result);
}

// function Call

numSum(10, 40);

numSum(92, 60);

numSum(32, 92);
