"use strict";
function calculateStatistics(numbers) {
    // Calculate and print the sum of the numbers
    var sum = numArray[0] + numArray[1] + numArray[2] + numArray[3] + numArray[4];
    console.log(sum);
}
// Example usage:
const numArray = [10, 5, 8, 20, 3];
calculateStatistics(numArray);
